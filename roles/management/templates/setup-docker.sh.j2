#!/bin/bash

# Add the GPG key for the official Docker repository to the system
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -

# Add the Docker repository to APT sources:
sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"

# update the package database
sudo apt-get update

# Ensure that the installation is from the Docker repo instead of the default Ubuntu 16.04 repo:
apt-cache policy docker-ce

# Install Docker Community Edition
sudo apt-get install -y docker-ce

# Output the status of the Docker Deamon
sudo systemctl status docker

# Add vagrant user to the docker group so that vagrant user can run docker without sudo
sudo usermod -aG docker ${USER}
